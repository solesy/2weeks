#include<stdio.h>
#include<stdlib.h>
#include<malloc.h>
#include<memory.h>
int quotient(int n);
int remainders(int n);
void countInit(int* count);
void query(int n, int* count);
void printCount(int* count);
